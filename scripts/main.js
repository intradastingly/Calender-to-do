window.addEventListener('load',main);

function main() {
    selectCalendarDate();
    eventListenersButtons();
    currentDate();
    startClock();
    addListIds();
    setCalenderDatesIds();
    populateDates()
}