window.addEventListener('load',main);

function main() {
    placeHolderArray()
    selectCalendarDate();
    eventListenersButtons();
    currentDate();
    startClock();
    addListIds();
    setCalenderDatesIds();
}